$folderPath = "E:\Carte\BB\17 - Site Leadership\alte\Ionel Balauta\Aryeht\Task 1 - Traduce tot site-ul\Doar Google Web\Andreea\Meditatii\2023\++Internet Archive BUN 2025 + Chrome\Cauta pe internet archive daca exista fisierele 4"

# Calea către fișierul de ieșire în același director cu folderul părinte
$outputDirectory = Split-Path -Path $folderPath -Parent
$outputFile = Join-Path -Path $outputDirectory -ChildPath "fisiere_problematice.txt"

# Obține doar fișierele din folderul rădăcină (fără recursivitate)
Get-ChildItem -Path $folderPath -File |
Where-Object { $_.Name -match '[-_]\s|\s[-_]' } |
Select-Object -ExpandProperty Name |
Out-File -FilePath $outputFile -Encoding utf8 -Force

Write-Host "Raportul a fost salvat în: $outputFile"
